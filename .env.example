# Copier ce fichier en .env.local et renseigner les valeurs ci-dessous.
# Ne JAMAIS committer de vraies valeurs ici : ce fichier est un template public.

# URL de base de l'API backend externe MyHub (sans slash final).
# Sert à tous les appels API admin (connexion, envoi OTP, missions, membres,
# whitelist, candidatures, stats).
# ⚠️ Renseigner l'URL exacte du backend du back-office (voir les variables d'env
# du projet sur la plateforme de déploiement).
NEXT_PUBLIC_BASE_URL=
